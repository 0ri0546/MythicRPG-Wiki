---
interface Props {
  src: string | null;
  alt: string;
  size?: 'small' | 'large';
}
const { src, alt, size = 'small' } = Astro.props;
---
<div class:list={['item-icon', `item-icon-${size}`]}>
  {src ? <img src={src} alt={alt} loading="lazy" /> : <span aria-hidden="true">?</span>}
</div>
